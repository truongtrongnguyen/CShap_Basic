namespace Dependency_inversion
{
    internal class Demo_ThuVien_Dependency_Injection
    {
        //DI contrainer ->cho phép đăng ký các kiểu lớp vào trong nó và hỗ trợ lấy ra các những đối tượng
    }
}
